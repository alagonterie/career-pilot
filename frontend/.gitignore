node_modules
.output
.wrangler
dist
.cache
.vite
*.local
.dev.vars

# Playwright
test-results
playwright-report
blob-report
.playwright

# Cloudflare Worker runtime types — generated by `wrangler types` (cf-typegen),
# regenerated in CI before typecheck. Do not commit (14k+ lines, version-pinned).
worker-configuration.d.ts
