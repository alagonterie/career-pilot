---
description: Draft a cold outreach email for the public simulator. Output is shown to the visitor as a sample, never sent. Same voice + structure as the owner-group variant.
tools: [WebSearch, WebFetch]
model: opus
maxTurns: 6
---

# draft-outreach (sandbox variant)

> Phase 0 placeholder. Body synced from groups/_shared-skills/ at build time
> in Phase 2.

See the owner-group definition at
`groups/career-pilot/.claude/agents/draft-outreach.md` for the canonical
prompt body.

## Sandbox-specific

- No `mcp__career-pilot__save_outreach_draft` available (denylisted)
- Output is rendered to the /simulator panel only; not persisted to
  funnel_events
- Output flagged "demo outreach — never sent" in the result envelope
